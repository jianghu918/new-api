# 本地开发启动指南

本文档说明如何在本地启动 new-api 的后端和前端，以及常用配置方式。

---

## 前置依赖

- **Go**：1.25+
- **Bun**：前端包管理器（优先于 npm/yarn/pnpm）
- **Docker & Docker Compose**：用于本地 PostgreSQL / Redis（可选，也可用 SQLite）

---

## 一、后端本地启动

### 1.1 创建前端 dist 占位目录

Go 编译时需要 `web/*/dist` 目录存在（即使为空），因为它们被 `//go:embed` 嵌入到二进制中。

```bash
mkdir -p web/default/dist web/classic/dist
```

### 1.2 使用 SQLite 启动（最简单，零依赖）

```bash
go run main.go
```

默认监听 `http://localhost:3000`，SQLite 数据库文件会自动创建在 `./data/new-api.db`。

### 1.3 使用 PostgreSQL + Redis 启动

```bash
SQL_DSN="postgresql://postgres:123456@localhost:5432/new-api" \
  REDIS_CONN_STRING="redis://localhost:6379" \
  go run main.go
```

如果本地没有 PostgreSQL / Redis，可以用 Docker Compose 一键启动：

```bash
docker compose up -d
```

### 1.4 常用后端环境变量

| 环境变量 | 说明 | 示例 |
|---------|------|------|
| `SQL_DSN` | 数据库连接串 | `postgresql://...` 或 SQLite 路径 |
| `REDIS_CONN_STRING` | Redis 连接串 | `redis://localhost:6379` |
| `TZ` | 时区 | `Asia/Shanghai` |
| `BATCH_UPDATE_ENABLED` | 启用批量更新 | `true` |
| `PORT` | 监听端口 | `3000` |
| `NODE_TYPE` | 节点类型（主/从） | `master` / `slave` |

---

## 二、前端本地启动

### 2.1 Default 前端（React 19 + Rsbuild）

```bash
cd web/default
bun install
bun run dev
```

- 开发服务器默认运行在 `http://localhost:3001`
- Rsbuild 会自动代理 `/api`、`/mj`、`/pg` 到 `http://localhost:3000`（后端）
- 修改代码后热更新（HMR）自动生效

### 2.2 Classic 前端（React 18 + Vite）

```bash
cd web/classic
bun install
bun run dev
```

### 2.3 前端构建（嵌入 Go 二进制）

```bash
# 构建 default 前端
cd web/default
bun run build

# 构建 classic 前端
cd web/classic
bun run build
```

构建产物输出到 `web/default/dist` 和 `web/classic/dist`，随后 `go build` 会将它们嵌入二进制。

---

## 三、一键启动完整开发栈

项目根目录提供了 `Makefile`，最推荐的开发方式：

```bash
# 启动后端服务（PostgreSQL + Redis） + 前端开发服务器
make dev

# 仅启动后端依赖服务
make dev-api

# 仅启动 default 前端开发服务器
make dev-web
```

---

## 四、Docker 构建与运行（可选）

### 4.1 构建 Docker 镜像

**Mac M1/M2/M3 (Apple Silicon) 构建 linux/amd64 镜像：**

```bash
# 构建前端（必须，因为 Go embed 需要 dist 目录）
cd web/default
bun install
bun run build
cd ../..

# 使用 buildx 构建 linux/amd64 镜像
docker buildx build --platform linux/amd64 -t new-api:dev .
```

**在 linux/amd64 机器上直接构建：**

```bash
docker build -t new-api:dev .
```

### 4.2 运行容器

```bash
docker run -d \
  --name new-api \
  --restart always \
  -p 3000:3000 \
  -e SQL_DSN="postgresql://postgres:123456@host.docker.internal:5432/new-api" \
  -e REDIS_CONN_STRING="redis://host.docker.internal:6379" \
  -e TZ=Asia/Shanghai \
  -e BATCH_UPDATE_ENABLED=true \
  -v $(pwd)/data:/data \
  new-api:dev
```

---

## 五、项目结构速查

```
router/        — HTTP 路由（API、relay、dashboard、web）
controller/    — 请求处理器
service/       — 业务逻辑
model/         — 数据模型与 DB 访问（GORM）
relay/         — AI API 代理层（provider 适配器）
  relay/channel/ — 各上游提供商适配（openai、claude、gemini、aws 等）
middleware/    — 认证、限流、CORS、日志、分发
setting/       — 配置管理
common/        — 公共工具（JSON、加密、Redis、环境变量等）
web/default/   — 默认前端（React 19、Rsbuild、Base UI、Tailwind）
web/classic/   — 经典前端（React 18、Vite、Semi Design）
```

---

## 六、常见问题

**Q: `go run main.go` 报错 `pattern web/default/dist: no matching files found`？**

A: 先执行 `mkdir -p web/default/dist web/classic/dist` 创建占位目录。

**Q: 前端请求 API 跨域？**

A: 开发模式下 Rsbuild/Vite 已配置代理，请确保前端 dev server 和后端在同一台机器，且使用 `bun run dev` 启动前端。

**Q: 路由文件报错或缺失？**

A: Default 前端使用 TanStack Router 文件系统路由，新增/修改 `src/routes/` 下的文件后，运行 `bun run dev` 会自动重新生成 `routeTree.gen.ts`。

